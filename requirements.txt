# GUI framework (modern themed widgets on top of tkinter)
# tkinter itself is included with Python standard library
customtkinter>=5.2.2
pillow>=10.0

# RTF parsing
striprtf>=0.0.29

# Version comparison for update checking
packaging>=23.0

# Additional utilities that may be needed
# xml.etree.ElementTree is built into Python
# sqlite3 is built into Python
# pathlib is built into Python
# uuid is built into Python
# re is built into Python

# Development and building
pyinstaller>=5.0